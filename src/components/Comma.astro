---
export interface Props {
    rotate?: boolean;
}

const { rotate = false } = Astro.props;
---

<h1
    class={`font-display text-8xl bg-gradient-to-r from-red to-yellow bg-clip-text text-transparent ${
        rotate && "rotate-180"
    }`}
>
    "
</h1>
